C program that serves as a simple shell which takes in commands from standard input and then executes them. 
This simple shell program is in an infinite loop, reading in commands, executing them, and then giving you a prompt.
